export enum DimensionEnum {
  Browser = 'browser',
  BusinessId = 'businessId',
  Channel = 'channel',
  Country = 'country',
  CreatedAt = 'createdAt',
  Currency = 'currency',
  Device = 'device',
  PaymentMethod = 'paymentMethod',
}
